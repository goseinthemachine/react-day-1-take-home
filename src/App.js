import React from "react";

const App = (movie) => {
    return React.createElement(
        "div",
        { className: "row"},
        React.createElement("h1", {}, "Hello World!"),
    );
};

export default App;
